import React from 'react'

function CartCard( {children,
    type = "button",
    bgColor = "bg-blue-600",
    textColor = "text-white",
    className = "",
    ...props}) {
  return (
    <div >
      
    </div>
  )
}

export default CartCard
